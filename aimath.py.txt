import sympy as sp

def solve_math(expression):
    try:
        result = sp.sympify(expression)
        return result
    except:
        return "Invalid expression"

while True:
    question = input("Enter a math problem: ")
    if question.lower() == "exit":
        break
    print("Answer:", solve_math(question))